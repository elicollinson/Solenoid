# Requirements mirror for environments that rely on pip sync.
# Prefer installing via `uv pip install -e .` to stay in sync with pyproject.toml.
litellm>=1.74
google-adk>=1.18.0
arize-phoenix>=12.9.0
sentence-transformers>=3.0.0
torch>=2.3.0
numpy>=1.26.0
sqlite-vec>=0.1.2
FlagEmbedding>=1.2.10
